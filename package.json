{
  "name": "pages-framework",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "utils/venv/bin/python utils/build.py",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "deploy:staging": "npm run build && wrangler deploy --env staging"
  },
  "devDependencies": {
    "wrangler": "^4.91.0"
  }
}
